#ifndef MY_MATH_FUNCTIONS_H
#define MY_MATH_FUNCTIONS_H
#include <complex>

void cross_product( const double* const vec1, const double* const vec2, double* const vec3);

long long int fact (long long int input);

long long int fact2 (long long int input);

double boys(double n, double T);

#endif
